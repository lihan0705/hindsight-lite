[project]
name = "hindsight-lite"
version = "0.1.0"
description = "Local-first memory for AI coding agents."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.0",
    "ruff>=0.14.0",
]
